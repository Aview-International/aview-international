import styles from './BlogContent.module.css';

function BlogContent() {
  <section className={`section m-horizontal`}>
    <h2 className={`section-title ${styles.title}`}></h2>
  </section>;
}

export default BlogContent;
